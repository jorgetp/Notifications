<resources>
    <!-- Base application theme. -->
    <style name="Theme.Notifications" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>